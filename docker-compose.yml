services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: speedybosh
      POSTGRES_PASSWORD: speedybosh
      POSTGRES_DB: speedybosh
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: